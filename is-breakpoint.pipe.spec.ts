import { IsBreakpointPipe } from './is-breakpoint.pipe';

describe('IsBreakpointPipe', () => {
  it('create an instance', () => {
    const pipe = new IsBreakpointPipe();
    expect(pipe).toBeTruthy();
  });
});
